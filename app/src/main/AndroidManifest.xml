<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rodrigobc.anotadordejuegosdemesa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".configurarTruco" />
        <activity android:name=".configurarChinchon" />
        <activity android:name=".configurarEscoba15" />
        <activity android:name=".configurarChancho" />
        <activity android:name=".configurarUno" />
        <activity android:name=".anotadorTruco" />
        <activity android:name=".MarcadorEscoba" />
        <activity android:name=".anotadorChinchon"></activity>
    </application>

</manifest>